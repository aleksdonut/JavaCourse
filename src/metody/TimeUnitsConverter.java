package metody;

public class TimeUnitsConverter {
    int hoursToMinutes(int x) {
        return x * 60;
    }

    int minutesToSeconds(int x) {
        return x * 60;
    }

    int secondsToMilliseconds(int x) {
        return x * 1000;
    }
}
